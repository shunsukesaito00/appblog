---
type Props = {
  title: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<div class="section-header">
  <h2>{title}</h2>
  {description && <p>{description}</p>}
</div>
